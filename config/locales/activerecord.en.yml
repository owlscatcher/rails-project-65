en:
  activerecord:
    models:
      bulletin: Bulletin
      bulletins: Bulletins
      category: Category
      categories: Categories
      user: User
      users: Users
    attributes:
      bulletin:
        title: Title
        description: Description
        category: Category
        user: User
        state: State
        states:
          draft: Draft
          published: Published
          archived: Archived
          rejected: Rejected
          under_moderation: Under moderation
        image: Image
        created_at: Added date
      category:
        id: ID
        name: Name
      user:
        id: ID
        name: Name
        email: Email
        bulletins: Bulletins
        admin: Admin?
    errors:
      messages:
        blank: can't be blank
        required: is required
        too_short: insufficient length (cannot be less than %{count} character)
        too_long: too long (cannot be longer than %{count} character)
        confirmation: doesn't match %{attribute}
        record_invalid: doesn't create a record
